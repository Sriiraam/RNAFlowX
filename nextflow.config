nextflow.enable.dsl = 2

params {
    sample_manifest = "data/metadata/sample_manifest.csv"

    outdir = "results"

    threads = 4
}

process {

    executor = 'local'

    cpus = 2

    memory = '4 GB'
}